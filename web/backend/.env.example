OPENAI_API_KEY=sk-xxx
OPENAI_BASE_URL=https://openrouter.ai/api/v1
MODEL_NAME=google/gemma-4-31b-it

# Set to "1" to run the real Playwright browser headed (visible window) for
# local debugging. Leave unset/"" for headless (default, used in production).
BROWSER_HEADED=
